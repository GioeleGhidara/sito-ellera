/**
 * Configurazione globale dello stato di manutenzione delle sezioni del sito.
 * Impostare a 'true' per attivare la vista di manutenzione su una specifica pagina.
 */
export const MAINTENANCE_CONFIG = {
  TREKKING: true,
  TRADIZIONI: true,
  EBIKE_FEST: true,
} as const;
